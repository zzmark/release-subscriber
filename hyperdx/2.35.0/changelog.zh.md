

## @hyperdx/app@2.35.0

### 小改动

- 88f62274：添加警报详细信息页面 (/alerts/:id)，并根据警报的查询绘制图表
 它的阈值、扩大的评估历史记录条和分页评估
 事件流（按组划分的警报的每组细分、评估分析
 列、时间范围范围的光标分页）。警报页面历史记录条
 呈现错误的评估窗口以及每个窗口的错误详细信息。后面有门
 NEXT_PUBLIC_ENABLE_ALERT_DETAILS（默认关闭）。
- 8508b6c7：Terraform 导出现在发出团队范围的导入 ID (`&lt;team_id&gt;/&lt;resource_id&gt;`)，
 因此可以从支持多个的 ClickStack 部署导入资源
 一支球队。每个导入的资源都会获得一个“团队”属性，该属性由提供者提供
 标记为强制替换 - 生成的文件现在表示保留它。的
 提供商楼层移动到 `>= 3.25.0`，这会在以下情况下删除仅服务器仪表板 ID
 导入，因此生成的仪表板配置不再搅动磁贴 ID（并且
 附加到它们的平铺警报）在应用时。
- 72269ece：将鼠标悬停在版本标记上现在会列出其集群中的每个版本
 发送它的服务、版本和时间。光靠颜色是不行的
 一旦图表的系列数量多于图例显示的数量，即可识别服务，并且
 崩溃的“N个版本”集群没有命名。
- 08b8783b：在仪表板图块图表上叠加发布标记，显示每个版本的发布时间
 服务首次出现，因此部署可以根据服务的变化进行调整
 数据。标记的范围为每个图块正在绘制的数据并着色以匹配
 他们的服务的系列颜色，并且在图表上被抑制
 绑定到可见线，因此跨越许多服务的聚合线不是
 带有注释的版本，您无法归因于它。
- d201b71f：添加可选的“serviceVersionExpression”来记录和跟踪源，识别
 服务的运行版本。默认为 OpenTelemetry
 `service.version` 资源属性；其发布标识符存在的团队
 其他地方，例如 GitOps 下的容器镜像标签，可以将其指向那里
 而不是更换仪器。

### 补丁更改



- 05a3fd81：添加 AlertHistory 评估读取模型和 GET /alerts/:id/evaluations
 端点：每个窗口的评估历史记录范围为一个时间范围（固定到
 保留窗口）以及针对分组警报、评估的每组细分
 分析字段、错误状态窗口的重复数据删除错误表面，以及
 基于光标的分页总是跨越间隙前进。添加读取端
 对错误状态 AlertHistory 行和评估分析的架构/类型支持。
- c46ddaee：从详细信息页面删除仪表板之前需要确认。
- b9430a62：壮举：将广播和变量设置添加到仪表板过滤器
- 546dd442：壮举：改进 SQL 编辑器验证和变量自动完成
- cab98c7c：壮举：替换原始 SQL 磁贴中的仪表板变量
- 90729734：使用“Ref”后缀一致地命名“useRef”值，并通过 ESLint 强制执行。 
重命名 10 个标记的参考（在“DOMPlayer”、“EditTimeChartForm”、“useMetadata”中）
 `sessions` 和 `utils`) 以 `Ref` 结尾，提升
 `@eslint-react/naming-convention/ref-name` 为 `error`，并降低应用程序的
 `--max-warnings` 上限。行为没有改变。
- 018a6486：清理 ESLint 警告并加强 lint 执行。全部解决
 `no-unused-vars` 和 `@typescript-eslint/ban-ts-comment` 警告（删除死的
 代码并将“@ts-ignore”转换为描述的“@ts-expect-error”），然后升级
 api/app/common-utils/cli/hdx-eval 配置中的“error”规则已禁用
 应用程序中嘈杂的“@typescript-eslint/no-empty-function”规则，并降低了每个
 包的“--max-warnings”上限，因此计数无法回归。行为是
 不变。
- 582f3940：在“加入团队”页面上显示密码要求，并使清单与服务器策略保持一致。当用户接受团队邀请并设置密码时，现在会显示身份验证/注册页面上使用的相同实时密码策略清单，因此用户不再需要猜测所需的长度、大小写、数字和特殊字符规则。之前的检查列表在两种方面与服务器不同，可以显示服务器拒绝的密码的全绿色检查：其特殊字符规则使用比后端更广泛的模式（因此唯一特殊字符为“~”、反引号或空格的密码通过了检查列表，但在提交时失败），并且它从未出现过 72 个字符的最大值（因此超长的密码通过了检查列表，但在提交时失败）。现在，长度规则强制执行最小值和最大值，并且密码策略检查（长度范围、大小写、数字和接受的特殊字符集）位于前端检查列表和后端“passwordSchema”使用的“@hyperdx/common-utils”中的单个共享模块中，因此它们不再漂移。最后，当服务器拒绝密码时，“加入团队”页面现在会显示失败的具体原因（例如“密码必须包含至少一个特殊字符 (!@#$%^&\*(),.?\":{}|<>;-+=)”），而不是一般的“密码无效”，因此用户会被准确告知要更改的内容 - 包括接受哪些特殊字符。
- 69a89aa9：修复：恢复 Lucene 自动完成
- aedb514f：多系列指标图表现在作为单个组合的 ClickHouse 查询运行，而不是在客户端加入的每个系列一个查询。每个系列的查询通过 UNION ALL 进行组合，并在 SQL 中转回到每个（组、时间桶）一行，包括比率图表（`seriesReturnType: 'ratio'`）和两个 `ratioMode` 变体，它们之前在浏览器/节点中划分了两个结果集。结果形状、列命名（包括相同别名 `__{index}` 消歧）、间隙语义和比率语义保持不变；具有多个系列的图表以更少的往返次数呈现，并且多系列指标图表的“查看 SQL”现在显示完整查询，而不仅仅是第一个系列。
- 463fd6a1：在旧版 JSON URL 参数中保留文字百分比序列。
- 更新了依赖项 [fd54ac78]
- 更新了依赖项 [05a3fd81]
- 更新了依赖项 [b9430a62]
- 更新了依赖项 [546dd442]
- 更新了依赖项 [cab98c7c]
- 更新了依赖项 [b6196031]
- 更新了依赖项 [de783063]
- 更新了依赖项 [018a6486]
- 更新了依赖项 [8508b6c7]
- 更新了依赖项 [582f3940]
- 更新了依赖项 [2d33b83b]
- 更新了依赖项 [4fa4975a]
- 更新了依赖项 [f891eb19]
- 更新了依赖项 [4c5ccfc4]
- 更新了依赖项 [6662379e]
- 更新了依赖项 [0ed72ddf]
- 更新了依赖项 [aedb514f]
- 更新了依赖项 [f34cfaed]
- 更新了依赖项 [d201b71f]



- 更新了依赖项 [711b905d]
- 更新了依赖项 [908b27ed]
 - @hyperdx/common-utils@0.26.0
 - @hyperdx/api@2.35.0

## @hyperdx/otel-collector@2.35.0

### 小改动

- 8351d632：为独立模式下的 OTLP 接收器添加基于 OIDC 的承载令牌身份验证，作为现有静态“OTLP_AUTH_TOKEN”的替代方案。设置“OIDC_ISSUER_URL”和“OIDC_AUDIENCE”以根据 OIDC 提供商发布的 JWKS（而不是单个长期共享密钥）验证传入的 OTLP 请求。

### 补丁更改

- 58a467ae：使用 OpAMP 管理程序的本机 `passthrough_logs` 作为收集器日志
 转发而不是后台“tail”进程。旧方法有
 主管和tailer写入相同的stdout fd，没有
 同步，因此日志行被两个流破坏
 中线交错。本机方法让主管重新发出
 收集器通过自己的记录器输出以避免这种情况。

## @hyperdx/common-utils@0.26.0

### 小改动

- fd54ac78：将警报评估错误（查询错误、超时、Webhook 失败）保留为
 错误状态警报历史记录而不是仅最新的快照，
 每个评估窗口都会更新插入，因此重试会折叠成一行。查询
 超时单独分类（QUERY_TIMEOUT，包括包装超时
 由 ClickHouse 查询客户端）和可操作的消息。错误行是
 排除在调度/回填计算之外，因此失败的窗口仍然存在
 重试并回填，一旦失败的窗口恢复（通过同一窗口
 重试或稍后的刻度回填）其过时的 ERROR 行将被删除。评价
 分析（查询/Webhook 持续时间、回填存储桶）记录在每个
 历史行。
- 05a3fd81：添加 AlertHistory 评估读取模型和 GET /alerts/:id/evaluations
 端点：每个窗口的评估历史记录范围为一个时间范围（固定到
 保留窗口）以及针对分组警报、评估的每组细分
 分析字段、错误状态窗口的重复数据删除错误表面，以及
 基于光标的分页总是跨越间隙前进。添加读取端
 对错误状态 AlertHistory 行和评估分析的架构/类型支持。
- 8508b6c7：Terraform 导出现在发出团队范围的导入 ID (`&lt;team_id&gt;/&lt;resource_id&gt;`)，
 因此可以从支持多个的 ClickStack 部署导入资源
 一支球队。每个导入的资源都会获得一个“团队”属性，该属性由提供者提供
 标记为强制替换 - 生成的文件现在表示保留它。的
 提供商楼层移动到 `>= 3.25.0`，这会在以下情况下删除仅服务器仪表板 ID
 导入，因此生成的仪表板配置不再搅动磁贴 ID（并且
 附加到它们的平铺警报）在应用时。
- 0ed72ddf：添加度量公式表达式模型：图表配置上的“公式”条目（基于字母的系列引用 - “A”、“B”、“C”映射到“选择”位置）加上仅算术解析器/验证器（“core/formula.ts”），该解析器/验证器会生成经过验证的 AST 和结构化验证错误（未知系列引用、空表达式、格式错误的语法、无效标记）。 “A / (A + B + C) * 100”等公制公式的基础；尚未查询渲染或 UI 更改。

### 补丁更改

- b9430a62：壮举：将广播和变量设置添加到仪表板过滤器
- 546dd442：壮举：改进 SQL 编辑器验证和变量自动完成
- cab98c7c：壮举：替换原始 SQL 磁贴中的仪表板变量
- 018a6486：清理 ESLint 警告并加强 lint 执行。全部解决
 `no-unused-vars` 和 `@typescript-eslint/ban-ts-comment` 警告（删除死的
 代码并将“@ts-ignore”转换为描述的“@ts-expect-error”），然后升级
 api/app/common-utils/cli/hdx-eval 配置中的“error”规则已禁用
 应用程序中嘈杂的“@typescript-eslint/no-empty-function”规则，并降低了每个
 包的“--max-warnings”上限，因此计数无法回归。行为是
 不变。
- 2d33b83b：在数字和布尔字段搜索中转义一次 Map 下标

 “Bool”和数值类型的三个相等分支转义了
 列表达式作为标识符，即使它已经是渲染的地图
 下标，因此 `Measures.latency_ms:250` 包含 `` `Measures`['latency_ms'] ``
 在第二层反引号中，ClickHouse 将其读取为一个标识符，而不是
 比地图查找。引用数字地图的术语；为了
 “Map(String, Bool)”列的两种拼写都会受到影响。



- aedb514f：多系列指标图表现在作为单个组合的 ClickHouse 查询运行，而不是在客户端加入的每个系列一个查询。每个系列的查询通过 UNION ALL 进行组合，并在 SQL 中转回到每个（组、时间桶）一行，包括比率图表（`seriesReturnType: 'ratio'`）和两个 `ratioMode` 变体，它们之前在浏览器/节点中划分了两个结果集。结果形状、列命名（包括相同别名 `__{index}` 消歧）、间隙语义和比率语义保持不变；具有多个系列的图表以更少的往返次数呈现，并且多系列指标图表的“查看 SQL”现在显示完整查询，而不仅仅是第一个系列。

## @hyperdx/api@2.35.0

### 小改动

- fd54ac78：将警报评估错误（查询错误、超时、Webhook 失败）保留为
 错误状态警报历史记录而不是仅最新的快照，
 每个评估窗口都会更新插入，因此重试会折叠成一行。查询
 超时单独分类（QUERY_TIMEOUT，包括包装超时
 由 ClickHouse 查询客户端）和可操作的消息。错误行是
 排除在调度/回填计算之外，因此失败的窗口仍然存在
 重试并回填，一旦失败的窗口恢复（通过同一窗口
 重试或稍后的刻度回填）其过时的 ERROR 行将被删除。评价
 分析（查询/Webhook 持续时间、回填存储桶）记录在每个
 历史行。
- 05a3fd81：添加 AlertHistory 评估读取模型和 GET /alerts/:id/evaluations
 端点：每个窗口的评估历史记录范围为一个时间范围（固定到
 保留窗口）以及针对分组警报、评估的每组细分
 分析字段、错误状态窗口的重复数据删除错误表面，以及
 基于光标的分页总是跨越间隙前进。添加读取端
 对错误状态 AlertHistory 行和评估分析的架构/类型支持。
- 4fa4975a：添加“clickstack_query_tiles”MCP 工具，用于验证许多仪表板图块
 一个电话。它接受仪表板 ID 和可选的磁贴 ID 列表
 （默认值：每个非 Markdown 磁贴），运行有界的磁贴查询
 并发性，并返回紧凑的每图块成功/失败摘要
 （状态、行数、错误和原始 SQL 宏警告）加上聚合
 计数。查询失败的图块会内联报告，而不会导致整个查询失败
 调用，因此代理可以通过一两次调用来验证整个仪表板
 每个图块调用一次“clickstack_query_tile”。 `clickstack_save_dashboard`
 指南现在指向用于保存后验证的批处理工具。
- d201b71f：添加可选的“serviceVersionExpression”来记录和跟踪源，识别
 服务的运行版本。默认为 OpenTelemetry
 `service.version` 资源属性；其发布标识符存在的团队
 其他地方，例如 GitOps 下的容器镜像标签，可以将其指向那里
 而不是更换仪器。

### 补丁更改



- b9430a62：壮举：将广播和变量设置添加到仪表板过滤器
- b6196031：将用户不再存在的会话视为已注销，而不是失败
 请求。删除团队成员会使该人的浏览器保留会话
 cookie 指向已消失的用户文档，并且“deserializeUser”报告
 将丢失的用户视为错误而不是未经身份验证的会话。因为
 `passport.session()` 在每个路由器之前运行，每个请求都携带
 cookie 返回 `500 出了问题:(` 无论路径或方法如何，
 包括公共路由，例如`POST /team/setup/:token`和`GET /logout`，所以
 被删除的人既不能接受新的邀请，也不能清除自己的邀请
 会议。过时的 ID 现已从会话中删除，请求将继续
 未经身份验证，因此受保护的路由回答 401 并且浏览器被发送回
 登录页面。
- de783063：清除剩余的小 api ESLint 警告并强制执行其规则。合并
 auth 中间件中重复的 Express `declare global` 命名空间块
 （需要`namespace`+空接口增强模式，所以它
 带有带有注释的范围禁用），并且范围“n/no-process-exit”关闭
 退出的进程入口点（`src/index.ts`、`src/tasks/index.ts`）
 带有状态代码的目的。 `@typescript-eslint/no-namespace`,
 `no-empty-object-type` 和 `n/no-process-exit` 被提升为 `error` 并且
 api `--max-warnings` 上限降低。行为没有改变。
- 018a6486：清理 ESLint 警告并加强 lint 执行。全部解决
 `no-unused-vars` 和 `@typescript-eslint/ban-ts-comment` 警告（删除死的
 代码并将“@ts-ignore”转换为描述的“@ts-expect-error”），然后升级
 api/app/common-utils/cli/hdx-eval 配置中的“error”规则已禁用
 应用程序中嘈杂的“@typescript-eslint/no-empty-function”规则，并降低了每个
 包的“--max-warnings”上限，因此计数无法回归。行为是
 不变。
- 582f3940：在“加入团队”页面上显示密码要求，并使清单与服务器策略保持一致。当用户接受团队邀请并设置密码时，现在会显示身份验证/注册页面上使用的相同实时密码策略清单，因此用户不再需要猜测所需的长度、大小写、数字和特殊字符规则。之前的检查列表在两种方面与服务器不同，可以显示服务器拒绝的密码的全绿色检查：其特殊字符规则使用比后端更广泛的模式（因此唯一特殊字符为“~”、反引号或空格的密码通过了检查列表，但在提交时失败），并且它从未出现过 72 个字符的最大值（因此超长的密码通过了检查列表，但在提交时失败）。现在，长度规则强制执行最小值和最大值，并且密码策略检查（长度范围、大小写、数字和接受的特殊字符集）位于前端检查列表和后端“passwordSchema”使用的“@hyperdx/common-utils”中的单个共享模块中，因此它们不再漂移。最后，当服务器拒绝密码时，“加入团队”页面现在会显示失败的具体原因（例如“密码必须包含至少一个特殊字符 (!@#$%^&\*(),.?\":{}|<>;-+=)”），而不是一般的“密码无效”，因此用户会被准确告知要更改的内容 - 包括接受哪些特殊字符。
- f891eb19：修复（mcp）：引导代理使用构建器查询工具而不是原始 SQL（HDX-4892）。遥测显示代理（笔记本调查）使用“clickstack_sql”进行约 73% 的数据查询——通常用于单源聚合、top-N 和构建器工具更可靠地表达的时间序列（原始 SQL 的错误率也约为 2 倍）。重新措辞了“clickstack_sql”描述，将其标记为最后的手段，向“clickstack_table”、“clickstack_timeseries”和“clickstack_search”添加了“更喜欢我而不是 SQL”的相互推动，并添加了服务器级“instructions”工具选择策略，以便在“initialize”上显示指南，而不仅仅是通过选择加入“query_guide”提示。
- 4c5ccfc4：向每个 MCP 工具添加 MCP 工具注释（readOnlyHint、 DestructiveHint），以便
 客户端可以区分只读查询工具和变异查询工具。读取/查询
 工具被标记为只读；保存/修补和删除工具被标记为具有破坏性
 因为它们可以覆盖或删除现有资源。提示将是



 相对于 MCP 规范默认值的冗余被省略（例如 structuralHint 是
 放弃只读工具，它没有任何意义）。
- 6662379e：壮举：通过 mcp 公开摘要指标
- f34cfaed：删除非功能性 `GET /ext/silence-alert/:token` 端点及其死代码路径。
- 711b905d：指导仪表板 MCP 代理过滤构建器图块（表、行、stacked_bar、
 数字、饼图、条形图），每个选择项上带有每个系列的“where”，其中
 图表编辑器显示为图块的可见“位置”框。仪表板提示
 并且选择项“where”工具描述现在转向它，并且保存
 和补丁工具拒绝这些上的tile-config-level `where`/`whereLanguage`
 带有可操作消息的类型（编辑器不会呈现图块级过滤器
 对于他们来说，所以它将是不可见且不可编辑的）。搜索、热图和
 event_patterns 磁贴保留其磁贴级别“where”。
- 908b27ed：拒绝源写入引用格式错误、缺失或其他团队的
 连接。
- 更新了依赖项 [fd54ac78]
- 更新了依赖项 [05a3fd81]
- 更新了依赖项 [b9430a62]
- 更新了依赖项 [546dd442]
- 更新了依赖项 [cab98c7c]
- 更新了依赖项 [018a6486]
- 更新了依赖项 [8508b6c7]
- 更新了依赖项 [2d33b83b]
- 更新了依赖项 [0ed72ddf]
- 更新了依赖项 [aedb514f]
 - @hyperdx/common-utils@0.26.0

